The program was written in C++11, build system used is CMake. Additional libraries used are as following:

PugiXML for parsing XML files
GLM for matrix calculations

Task list (completed tasks are marked)
[X] Output a valid black image file
[X] Be able to read in the xml file of the specification.
[ ] Output an image with spheres on it after ray tracing them.
[X] Be able to read in geomtery from the specified xml files. (example1.xml)
[ ] Implement basic Phong illumination and shading. (example2.xml)